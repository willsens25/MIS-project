/**
 * Kontrol Visibilitas Fitur Data Dummy / Demo
 * 
 * PENGATURAN MODE PENGUJIAN:
 * - Diset `true` agar tombol dan pintasan data dummy aktif dan dapat dicoba
 *   baik di development maupun di shared preview URL.
 * - Ketika Anda siap untuk mempublikasikan (publish) secara resmi tanpa data dummy,
 *   cukup ubah nilai ini menjadi `false` (atau `import.meta.env.DEV`).
 */
export const SHOW_DUMMY_BUTTON: boolean = true;

